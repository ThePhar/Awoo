enum RecognisedCommands {
    Join = "join",
    Leave = "leave",
    Rules = "rules",
    Accuse = "accuse",
    Target = "target",
    Role = "role",
    Kill = "kill",
    Save = "save",

    SetChannel = "setchannel",
}

export default RecognisedCommands;
