enum RecognisedCommands {
    Join = "join", // Complete
    Leave = "leave", // Complete
    Status = "status",
    Rules = "rules",
    Accuse = "accuse",
    Kill = "kill",
    Inspect = "inspect",
}

export default RecognisedCommands;
